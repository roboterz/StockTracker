<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 按钮被选中时的颜色 -->
    <item android:color="#2689FE" android:state_checked="true" />
    <!-- 按钮未被选中时的颜色 (透明) -->
    <item android:color="@android:color/transparent" />
</selector>
